
/* Sprint 4 Task 2 — Creative Recommendation Engine */
.recommendation-card {
  display: grid;
  gap: 10px;
  margin-bottom: 14px;
}

.recommendation-section,
.recommendation-grid > div {
  padding: 10px;
  border: 1px solid rgba(110, 185, 216, 0.12);
  border-radius: 9px;
  background: rgba(4, 14, 24, 0.68);
}

.recommendation-section > span,
.recommendation-grid span {
  display: block;
  margin-bottom: 5px;
  color: #6f8fa1;
  font-size: 8px;
  font-weight: 800;
  letter-spacing: 0.11em;
  text-transform: uppercase;
}

.recommendation-section strong,
.recommendation-grid strong {
  color: #dff8ff;
  font-size: 10px;
  line-height: 1.4;
  text-transform: capitalize;
}

.recommendation-section p {
  margin: 0;
  color: #91afbf;
  font-size: 9px;
  line-height: 1.5;
}

.hero-recommendation {
  border-color: rgba(32, 215, 255, 0.28);
  background:
    linear-gradient(135deg, rgba(32, 215, 255, 0.08), transparent),
    rgba(4, 14, 24, 0.78);
}

.hero-recommendation strong {
  display: block;
  color: #f0fbff;
  font-size: 13px;
  line-height: 1.2;
  text-transform: uppercase;
}

.recommendation-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 8px;
}

.palette-preview {
  display: flex;
  align-items: center;
  gap: 6px;
}

.palette-preview i {
  width: 20px;
  height: 20px;
  border: 1px solid rgba(255, 255, 255, 0.12);
  border-radius: 6px;
}

.palette-preview strong {
  margin-left: 4px;
  font-size: 9px;
}

.prompt-section {
  max-height: 122px;
  overflow-y: auto;
}

.prompt-section p {
  font-family: "Cascadia Code", Consolas, monospace;
  font-size: 8px;
}
