export type RecentProject={name:string;filePath:string|null;updatedAt:string};
export function HomeScreen({projectName,canContinue,recent,onContinue,onTemplates,onNew,onRecent}:{projectName:string;canContinue:boolean;recent:RecentProject[];onContinue:()=>void;onTemplates:()=>void;onNew:()=>void;onRecent:(item:RecentProject)=>void}) {
  return <div className="home-screen"><header><p className="eyebrow">CYBERSLIDE STUDIO</p><h1>What do you want to create?</h1></header><div className="home-actions"><button className="home-primary" disabled={!canContinue} onClick={onContinue}><strong>Continue Last Project</strong><span>{projectName}</span></button><button onClick={onTemplates}><strong>Browse Production Templates</strong><span>Start with a production-ready format</span></button><button onClick={onNew}><strong>New Project</strong><span>Begin from a blank script</span></button></div><section className="recent-projects"><h2>Recent Projects</h2>{recent.length?recent.map(item=><button key={`${item.filePath}-${item.updatedAt}`} onClick={()=>onRecent(item)}><strong>{item.name}</strong><span>{new Date(item.updatedAt).toLocaleString()}</span></button>):<p>No recent projects yet.</p>}</section></div>;
}
