import type { ProductionTemplate } from "../library/types";import type { QualityResult,RewriteResult } from "./types";import { scoreTemplate,similarity } from "./ScriptQualityScorer";
export class TemplateRewriteService{private accepted:ProductionTemplate[]=[];validate(template:ProductionTemplate):QualityResult{const quality=scoreTemplate(template);if(this.accepted.some(item=>item.hook===template.hook))quality.errors.push("Duplicate hook.");if(this.accepted.some(item=>similarity(item.voiceScript,template.voiceScript)>.72))quality.errors.push("Voice script is too similar to an accepted template.");return quality;}accept(template:ProductionTemplate):RewriteResult{const quality=this.validate(template);if(quality.errors.length)throw new Error(`Template rejected: ${quality.errors.join(" ")}`);this.accepted.push(template);return {template,quality,attempts:1};}}
